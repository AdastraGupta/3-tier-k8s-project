apiVersion: v1
kind: Service
metadata:
  name: frontend-svc
  namespace: taskmanager
  labels:
    app: taskmanager
    tier: frontend
spec:
  type: ClusterIP
  selector:
    app: taskmanager
    tier: frontend
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
