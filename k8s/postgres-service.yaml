apiVersion: v1
kind: Service
metadata:
  name: postgres-svc
  namespace: taskmanager
  labels:
    app: taskmanager
    tier: database
spec:
  type: ClusterIP
  selector:
    app: taskmanager
    tier: database
  ports:
    - port: 5432
      targetPort: 5432
      protocol: TCP
